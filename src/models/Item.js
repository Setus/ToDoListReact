class Item {

    itemId;
    itemName;
    done;

    constructor(itemId, itemName, done) {
        this.itemId = itemId;
        this.itemName = itemName;
        this.done = done;
    }
}

export default Item;